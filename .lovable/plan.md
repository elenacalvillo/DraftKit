

## Replace Favicon

1. Copy the uploaded favicon to `public/favicon.ico`
2. Update `index.html` to explicitly reference it with a `<link rel="icon">` tag (currently there's no `<link rel="icon">` in the HTML head, so browsers may fall back to defaults)

